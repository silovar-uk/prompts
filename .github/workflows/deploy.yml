name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Generate and validate prompt references
        run: npm run catalog

      - name: Evaluate search quality
        run: npm run test:search

      - name: Run unit tests
        run: npm run test

      - name: Type-check application and scripts
        run: npx tsc -b

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Generate core catalog
        run: npm run generate:core

      - name: Add image and study prompts
        run: npm run extend:catalog

      - name: Generate readable prompt pages
        run: npm run build:references

      - name: Polish prompt pages
        run: npm run polish:references

      - name: Validate generated references
        run: npm run validate:references

      - name: Build deploy artifact
        env:
          VITE_COMMIT_SHA: ${{ github.sha }}
        run: npx vite build

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
